<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport"
          content="width=device-width, initial-scale=1, minimal-ui"
    >

    <!-- iOS PWA behaviour -->
    <meta name="apple-mobile-web-app-title" content="All Ears">
    <meta name=”apple-mobile-web-app-capable” content=”yes”>
    <meta name="apple-mobile-web-app-status-bar-style" content="black">

    <!-- Chrome PWA behaviour -->
    <meta name="mobile-web-app-capable" content="yes">

    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-D02SJSE21V"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-D02SJSE21V', {
        cookie_prefix: 'all_ears'
      });
    </script>

    <!-- TODO: How to use the title and description from a page's frontmatter if it has one -->
    <title>All Ears - A New Concept For Exhibition Audio Guides</title>
    <meta
      name="description"
      content="All Ears creates exhibition audio guides that tell the story of art from a variety of perspectives and for everyone, whether you know loads about art, or nothing at all. 🖼🧐"
    >

    <!-- Open Graph meta tags for social media -->
    <meta property="og:title" content="All Ears - A New Concept For Exhibition Audio Guides">
    <meta property="og:description" content="All Ears creates exhibition audio guides that tell the story of art from a variety of perspectives and for everyone, whether you know loads about art, or nothing at all. 🖼🧐">
    <meta property="og:image" content="http://www.allears.art/favicon.png">
    <meta property="og:url" content="https://www.allears.art">

    <!-- Favicons -->
    <%= favicon_link_tag 'favicon/favicon.ico' %>
    <% [57,72,60,76].each do |size| %>
      <%= favicon_link_tag "favicon/apple-touch-icon-#{size}x#{size}.png", rel: 'apple-touch-icon', type: 'image/png', sizes: "#{size}x#{size}" %>
      <%= favicon_link_tag "favicon/apple-touch-icon-#{size * 2}x#{size * 2}.png", rel: 'apple-touch-icon', type: 'image/png', sizes: "#{size * 2}x#{size * 2}" %>
    <% end %>
    <% [16,32,96,196].each do |size| %>
      <%= favicon_link_tag "favicon/favicon-#{size}x#{size}.png", type: 'image/png', sizes: "#{size}x#{size}" %>
    <% end %>
    <%= favicon_link_tag "favicon/favicon-128.png", type: 'image/png', sizes: "128x128" %>
    <% [70,150,310].each do |size| %>
      <meta name="<%= "msapplication-square#{size}x#{size}logo"%>" content="<%= asset_path("favicon/mstile-#{size}x#{size}.png")%>" />
    <% end %>
    <meta name="application-name" content="All Ears"/>
    <meta name="msapplication-TileColor" content="#FFFFFF" />
    <meta name="msapplication-wide310x150logo" content="<%= asset_path("favicon/mstile-310x150.png")%>" />
    <meta name="msapplication-TileImage" content="<%= asset_path("favicon/mstile-144x144.png")%>" />

    <!-- Rails security tags -->
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Run Rails stylesheets -->
    <%= stylesheet_link_tag 'application', media: 'all' %>

    <!-- Run React entrypoint 'application' pack -->
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
  </head>

  <body>
    <%= yield %>
    <%= yield :after_js %>
  </body>
</html>
